name: Create preview builds
jobs:
  android_preview_build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: preview
  ios_device_preview_build:
    name: Build iOS device
    type: build
    params:
      platform: ios
      profile: preview
