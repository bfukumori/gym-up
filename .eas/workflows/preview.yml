# Create preview builds
#
# Builds Android and iOS preview builds for devices and simulators.
# Learn more: https://docs.expo.dev/develop/preview-builds/introduction/
#
# Created by EAS CLI v22.0.0

name: Create preview builds
jobs:
  android_preview_build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: preview
  ios_device_preview_build:
    name: Build iOS device
    type: build
    params:
      platform: ios
      profile: preview
